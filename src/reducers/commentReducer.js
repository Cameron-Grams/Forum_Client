import * as actionTypes from '../actions/actionTypes'; 



const initialState = {
    topicComments: []
}

// topicComments should be an array of comment objects retreived after the topic is selected

const CommentReducer = ( state = initialState, action ) => {
    switch( action.type ){
        case value:

            break

        default:
            break
    }
}